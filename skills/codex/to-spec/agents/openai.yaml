interface:
  display_name: "To Spec"
  short_description: "Turn a conversation into a spec"
  default_prompt: "Use $to-spec to synthesize this work into an approved project specification."
policy:
  allow_implicit_invocation: false
