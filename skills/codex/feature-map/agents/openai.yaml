interface:
  display_name: "Feature Map"
  short_description: "Map product capabilities to repository evidence"
  default_prompt: "Use $feature-map to map this project's durable capabilities, flows, dependencies, and verification evidence."
policy:
  allow_implicit_invocation: true
