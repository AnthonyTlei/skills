# Feature map

This directory contains the project's canonical product-capability map. Edit `config.json`, `features/`, and `flows/`; regenerate the root `FEATURE_MAP.md` and `generated/` views with the installed `feature-map` skill.

Coverage applies only to the scope declared in `config.json`. File presence does not prove implementation or verification.

## Discovery coverage

Inspection revision or date: Not yet inspected.

| Surface or domain | Inspected entry points | Mapped IDs | Omissions or uncertainty |
| --- | --- | --- | --- |
| Declared scope | Not yet inspected | None | Discovery pending |

Update this ledger during discovery or when coverage changes. It supports the coverage claim; validation does not verify its completeness.
