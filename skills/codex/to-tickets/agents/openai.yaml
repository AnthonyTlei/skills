interface:
  display_name: "To Tickets"
  short_description: "Split a plan into tracer-bullet tickets"
  default_prompt: "Use $to-tickets to turn this plan into approved implementation tickets."
policy:
  allow_implicit_invocation: false
